pipeline {
    agent any
    stages {
        stage('remote') {
            steps {
                sh "ssh 147.50.227.54"
                sh "root"
                sh "PIyapanadmin"
            }
        }
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
        stage('test-deploy') {
            steps {
                sh "pwd"
                sh "ls -la"
                sh "kubectl apply -f nginx-deployment.yaml"
            }
        }
        stage('Hello-success') {
            steps {
                echo 'pipeline success'
            } 
        }
    }
}
